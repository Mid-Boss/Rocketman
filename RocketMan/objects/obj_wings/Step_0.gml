/// @description Insert description here
// You can write your code in this editor

if (global.wing_1_installed && global.wing_2_installed)
{
	with (obj_factory)
	{
		instance_destroy();
	}
	if x < room_width/2
		instance_destroy();
}